<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo_general"
    android:orientation="vertical">

    <!-- CABECERA PRINCIPAL ESTILO CAPTURA -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarCategorias"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/fondo_card"
        app:navigationIcon="@drawable/icono_regresar_pantalla"
        app:navigationIconTint="@color/texto_principal"
        app:title="Categorías"
        app:titleTextColor="@color/texto_principal"
        app:titleTextAppearance="@style/TextAppearance.MaterialComponents.Headline6">

        <ImageButton
            android:id="@+id/btnAgregarCategoria"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="end|center_vertical"
            android:layout_marginEnd="12dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Nueva categoría"
            android:src="@drawable/icono_boton_agregar"
            app:tint="@color/texto_principal" />
    </com.google.android.material.appbar.MaterialToolbar>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E2E8F0" />

    <!-- SUBTÍTULO INFORMATIVO -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="10dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvTotalCategoriasLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Carpetas de clasificación"
            android:textColor="@color/texto_secundario"
            android:textSize="13sp" />

        <TextView
            android:id="@+id/tvTotalCategoriasCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="6 categorías"
            android:textColor="@color/morado_scanner"
            android:textSize="13sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- LISTA DE CARPETAS -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCategoriasCarpetas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:paddingTop="6dp"
        android:paddingBottom="24dp" />

</LinearLayout>
